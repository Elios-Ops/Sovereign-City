# Scripts

Repository verification and inventory tooling.

- `inventory-tree.sh` — display the repository tree excluding generated files
- `verify-repository.sh` — verify required directories, documentation, and repository health

Neither script modifies files.
