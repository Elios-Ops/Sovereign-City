# Ecosystem Topology

STATUS:
Authoritative source not yet imported. This document will be populated as applications are recovered, verified, and their connections traced (see `CONNECTION_REGISTRY.md`).

## Known Architectural Principle

One repository. Multiple independent applications, connected through navigation (links, `window.open()`, external deployment URLs) rather than a shared frontend or router.
